Run this on Flask, you can also install requirements using requirements.txt
pip install -r requirements.txt
(assumes you have anaconda installed with nltk, scikit-learn etc.)

Once server Runing just go to browser - localhost:5000
